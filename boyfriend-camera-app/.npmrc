# 解决 EAS Build 中 @react-native-community/datetimepicker 与 expo@55 preview 的 peer 冲突
# 使 npm ci 使用 legacy-peer-deps，与本地 pnpm 的宽松解析行为一致
legacy-peer-deps=true

# 解决 pnpm 与 CocoaPods 的兼容性问题（PrivacyInfo.xcprivacy 等资源文件路径问题）
shamefully-hoist=true
